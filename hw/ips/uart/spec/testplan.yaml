schema: 1
ip: uart
qualification:
  required_evidence: [lint, functional, traceability, cdc_rdc, formal]
items:
  - id: UART-TP-001
    requirements: [UART-RST-001, UART-LOOP-001, UART-STAT-001, UART-STAT-002, UART-LOOP-002]
    methods: [formal, simulation]
    tests: [smoke, line_loopback]
    properties: [uart_prove]
  - id: UART-TP-002
    requirements: [UART-RX-001]
    methods: [simulation]
    tests: [rx_fifo]
  - id: UART-TP-003
    requirements: [UART-PAR-001]
    methods: [simulation]
    tests: [parity_reconfig]
  - id: UART-TP-004
    requirements: [UART-FILT-001]
    methods: [simulation]
    tests: [noise_filter]
  - id: UART-TP-005
    requirements: [UART-REG-001]
    methods: [simulation]
    tests: [reconfig, corners, auto_toggle, random_seed_1, random_seed_2]
