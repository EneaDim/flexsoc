schema: 1
ip: gpio
qualification:
  required_evidence: [lint, functional, traceability, cdc_rdc, formal]
items:
  - id: GPIO-TP-001
    requirements: [GPIO-RST-001, GPIO-OUT-001, GPIO-OE-001, GPIO-HOLD-001, GPIO-IN-001, GPIO-INT-001]
    methods: [formal, simulation]
    tests: [smoke, interrupt_levels]
    properties: [gpio_prove]
  - id: GPIO-TP-002
    requirements: [GPIO-FILT-001]
    methods: [simulation]
    tests: [input_filter]
  - id: GPIO-TP-003
    requirements: [GPIO-REG-001]
    methods: [simulation]
    tests: [reconfig, corners, auto_toggle, random_seed_1, random_seed_2]
