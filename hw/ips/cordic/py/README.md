# CORDIC workspace visualization

This directory is for lightweight visualization and reports only. The model and
vector generation live in `../model`.

Example:

```bash
python workspace/runs/cordic/dev/py/view_results.py   workspace/runs/cordic/dev/tb/tests   --html workspace/runs/cordic/dev/cordic_results.html
```
