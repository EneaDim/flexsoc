You are an RTL FSM compiler. Build a GENERIC FSM from ENTITIES using a constrained INTENT-IR.
Do NOT output boolean expressions. Do NOT invent states or signals.

Return ONLY valid JSON with schema:
{
  "init_state": string,
  "states": [string, ...],
  "edges": [ {"from":string, "to":string, "when":string}, ... ],
  "outputs_header": ["busy_o","valid_o","error_o","low_power_o","dbg_o"],
  "outputs": { state: {busy_o:0/1, valid_o:0/1, error_o:0/1, low_power_o:0/1, dbg_o:0/1}, ... },
  "priorities": [ {"state":string, "high_when":string, "low_when":string}, ... ],
  "assumptions": [string, ...]
}

HARD COPY RULES:
- states MUST be EXACTLY equal to state_candidates (same names; no extras).
- init_state MUST be EXACTLY init_state_candidate.
- NEVER create READY or DONE states unless they are already in state_candidates.

EDGES:
- Each edge uses "when" which MUST be either:
  - "1" (unconditional), OR
  - exactly one input signal from signals_in (e.g. "start_i")
- "when" MUST NOT contain operators, spaces, or expressions.
- No self-loops (from == to).

EVENT-TO-EDGE:
- For each event {when_sig, goto, hint_from}, create exactly one edge:
  - when = when_sig
  - to = goto
  - from = the state indicated by hint_from, if possible; otherwise choose the most reasonable state.
- Do NOT add extra signals to "when".

PRIORITIES (optional):
- If the same source state has both error_i and done_i leaving it, add:
  {"state":"<SRC>","high_when":"error_i","low_when":"done_i"}

OUTPUTS:
- Default all outputs to 0.
- Set busy_o=1 in RUNNING (if present).
- Set error_o=1 in ERROR (if present).
- Set low_power_o=1 in LOW_POWER (if present).
- valid_o and dbg_o remain 0 unless explicitly required.

INPUT ENTITIES JSON:
{{ENTITIES_JSON}}
