name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  packages: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: FlexSoC tests in locked container
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Validate immutable image lock
        id: image
        shell: bash
        run: docker/scripts/check-lock.sh

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull locked image
        run: docker pull "${{ steps.image.outputs.ref }}"

      - name: Cache project PDK downloads
        uses: actions/cache@v4
        with:
          path: .flexsoc/pdks
          key: flexsoc-pdks-${{ runner.os }}-${{ hashFiles('pdks/**', 'src/flexsoc/pdk.py') }}
          restore-keys: |
            flexsoc-pdks-${{ runner.os }}-

      - name: Run lint, API tests, and E2E in the image
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          FULL_E2E: "1"
        run: docker/scripts/run-ci.sh "$IMAGE_REF"
